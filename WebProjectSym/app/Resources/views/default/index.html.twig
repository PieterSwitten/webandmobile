{% extends 'base.html.twig' %}
{% block title %}homepage{% endblock %}
{% block body %}
    <h1>Login</h1>
    <p>Ingelogd als
        {{ app.User  }}
        {% if is_granted('ROLE_ADMIN') %}
            (ADMIN)
        {% elseif is_granted('ROLE_USER') %}
            (USER)
        {% else %}
            (Anonymous)
        {% endif %}
    </p>
    <div>
        <a href={{ path("quitroute") }}> quit</a>
        <a href={{ path("userroute") }}> user</a>
        <a href={{ path("adminroute") }}> admin</a>
    </div>
{% endblock %}